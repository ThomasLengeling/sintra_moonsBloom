
enum{
    INTRO
};

void MoonsApp::timeToggle()
{
	/*
	if (activateEvent){
	switch (T0GGLE_MODE) {
	case INTRO:
	break;
	}
	}
	*/

}


void MoonsApp::timeActivator()
{
	//int timeTrack;
   // int timeTrack = (int)mParticleSystemReactive->getCurrentTime();
  //  console()<<mAudio->beatValueLeft<<" left"<<std::endl;
    
  /*  if(timeTrack == 1){
        if(timeTrack != eventTimePrev){
            console()<<"INTRO 1 ACTIVATOR"<<std::endl;
            activateEvent = true;
            T0GGLE_MODE   = INTRO;
            eventTimePrev = timeTrack;
        }
    }
*/
    timeToggle();
}